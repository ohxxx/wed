[package]
name = "crypto-python"
version = "0.1.1"
edition = "2021"

[lib]
name = "wedpy"
crate-type = ["cdylib"]

[dependencies]
crypto-core = { path = "../crypto-core" }
pyo3 = { version = "0.22", features = ["extension-module"] }
